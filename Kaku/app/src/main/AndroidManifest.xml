<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.write.kaku.kaku">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoadingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ManualActivity"></activity>
        <activity android:name=".LoginActivity"></activity>
        <activity android:name=".SignupIdActivity"></activity>
        <activity android:name=".SignupPwActivity"></activity>
        <activity android:name=".SignupNnActivity"></activity>
        <activity android:name=".PasswordActivity"></activity>
        <activity android:name=".WriteActivity"></activity>        <activity
            android:name=".MainViewActivity"
            android:label="@string/title_activity_main_view"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>